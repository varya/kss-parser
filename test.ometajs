ometa kssDivider {

  inlineComment = seq('//') (~seq('\n') char)*:x seq('\n') -> x.join(''),

  kssMarkup = inlineComment (~seq('\n\n') char)*:x seq('\n\n') -> [#kss, x.join('')],

  blockEnd = kssMarkup | end,

  block = kssMarkup (~blockEnd char)*:x -> [#block, x.join('')],

  any = (block)*

}
